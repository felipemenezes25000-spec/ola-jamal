# RenoveJá Mobile App - Environment Variables Example
# Copy this file to .env and update with your values

# URL do Backend (obrigatório para app no celular físico ou Expo tunnel)
# - Mesma Wi‑Fi: use o IP do PC (ex: http://192.168.15.83:5000). Veja COMO-ABRIR-NO-CELULAR.md
# - Qualquer rede (4G, outra Wi‑Fi): use ngrok. Rode ngrok http 5000 e depois:
#     .\scripts\atualizar-env-ngrok.ps1
#   A URL do ngrok free muda a cada sessão; o script atualiza o .env com a URL atual. Veja REDE-QUALQUER.md
# - Emulador Android: 10.0.2.2:5000 (já é o fallback)
# - Web: http://localhost:5000
EXPO_PUBLIC_API_URL=http://192.168.15.83:5000

# Para produção, use:
# EXPO_PUBLIC_API_URL=https://api.renoveja.com.br

# Login com Google (obrigatório para o botão "Entrar com Google")
# Crie um "ID do cliente OAuth 2.0" tipo "Aplicativo da Web" no Google Cloud Console
# e adicione os redirect URIs: renoveja://auth e com.renoveja.app:/auth
# Guia completo: GOOGLE_OAUTH_SETUP.md
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=SEU_CLIENT_ID.apps.googleusercontent.com
